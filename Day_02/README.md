# Variables
containers that store items or collections of items or data

## types of Data type 

`String`
`boolean` True  or false 
`int` whole number
`float` Decimal numbers
`list` store collection of different data
`turple` store collection of data as but it not  mutable 

## Type conversion
we can convert one data tye to another 
 ### To string
 `string()`
 ### To Int
 `int()`
 ### To float
 `float()`

 ## Formattting String

`-` we can use  f string and additional operator to bring string together 