# Python modules and libraries
modules is python files while library is a collection of modules

# Bonus Challenge 
1. Allowed user to specify character to include or exclude
2. allowed user to generate multiple password at once insted of one 
3. Save generated password in a file 